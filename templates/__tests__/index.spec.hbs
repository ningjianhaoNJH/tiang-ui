import { mount } from '@vue/test-utils'
import Tiang{{hump name}} from '@tiang-ui/{{name}}'

describe('@tiang-ui/{{name}}', () => {
  it('create', () => {
    const wrapper = mount(Tiang{{hump name}})
    expect(wrapper.classes()).toContain('tiang-{{name}}')
  })
})
